services:
  db:
    ports:
      - '${POSTGRES_PORT}:5432'

  api:
    build:
      target: development
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
